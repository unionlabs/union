---
title: Livee
---
import { Code } from 'astro:components'
import { Processable as RProcessable } from '#/components/react/Processable'

fop


  <RProcessable
    client:load
    props={{
      fn: async () => {
        const response = await fetch('https://hubble-purple.hasura.app/v1/graphql', {
          method: 'POST',
          body: JSON.stringify({
            query: `query ClientsQuery {
  data: v1_clients {
    chain {
      rpc_type
      chain_id
    }
    client_id
    counterparty_chain_id
  }
}`
          }),
          headers: { 'Content-Type': 'application/json' },
        })
        if (!response.ok) return
        const data = await response.json()
        console.info(data)
      },
      key: 'yo',
      warning: 'yo',
    }}
  >
    <div>yo</div>
</RProcessable>
