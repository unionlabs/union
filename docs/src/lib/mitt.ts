import mitt from "mitt"

// export const emitter = mitt()

// // listen to an event
// emitter.on("foo", e => console.log("foo", e))

// // listen to all events
// emitter.on("*", (type, e) => [console.info('mitt'),console.log(type, e)])
