---
const { class: className, padded, ...rest } = Astro.props;
---

<div class:list={["from-black bg-gradient-to-b to-neutral-950 rounded border border-[#1f1f1f] shadow-lg size-full", {"p-4 md:p-6": padded}, className]} {...rest} >
  <slot/>
</div>
