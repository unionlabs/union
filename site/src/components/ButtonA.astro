---
const { href, fake } = Astro.props
const Element = fake ? "div" : "a"
---

<Element class="bg-accent-500 md:h-20 flex items-center justify-center md:text-3xl font-bold text-black capitalize px-4 md:px-8 py-2 w-fit" {href}>
  <slot />
</Element>

