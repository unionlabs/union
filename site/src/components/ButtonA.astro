---
const { href, fake, ...props } = Astro.props
const Element = fake ? "div" : "a"
---

<Element
  class="pointer-events-auto bg-black flex items-center justify-center text-white border px-6 py-2 w-fit font-semibold font-mono uppercase text-md md:text-lg"
  {href}
  {...props}
>
  <slot /> >>>
</Element>
