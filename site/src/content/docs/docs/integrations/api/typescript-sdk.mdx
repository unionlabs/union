---
title: "TypeScript SDK"
sidebar:
  order: 1


---
import "addreth/styles.css"
import '#/lib/wagmi-polyfills'
import Code from '#/components/Code.astro'
import TypeScriptSdkDemo from "#/components/ts-sdk"
import { Addreth, AddrethConfig } from "addreth/no-wagmi"
import { bech32AddressToHex } from "../../../../../../../typescript-sdk/src/convert.ts"

<Code
  lang="tsx"
  meta="{1,3} line:1-3,5-6"
  code={
`// @errors: 2322

const foo = 'hello'  // [!code --]
const bar: number = '1'
console.info(foo)
`}
/>

{/* <TypeScriptSdkDemo client:load /> */}

```tsx
import { custom } from "viem"
import * as React from "react"
import { sepolia } from "viem/chains"

```
```tsx live props={{ }} 
import { custom } from "viem"
import * as React from "react"
import { sepolia } from "viem/chains"
import { Addreth, AddrethConfig } from "addreth/no-wagmi"
import { createCosmosSdkClient as createUnionClient } from "@union/client"

export default () => {
  const [connected, setConnected] = React.useState(false)
  const [account, setAccount] = React.useState<`0x${string}` | undefined>(undefined)
  const [client, setClient] = React.useState<ReturnType<typeof createUnionClient>>()

  React.useEffect(() => {
    if (!window.ethereum) return
    const client = createUnionClient({
      evm: {
        account,
        chain: sepolia,
        transport: custom(window.ethereum)
      }
    })
    setClient(client)
  }, [account])

  const onConnectClick = async (event: React.MouseEvent<HTMLButtonElement>) => {
    event.preventDefault()
    if (connected) return [setAccount(undefined), setConnected(false)]

    const [account] = await client.request({ method: "eth_requestAccounts" })
    return [setAccount(account), setConnected(true)]
  }

  return (
    <main className="flex flex-col items-center justify-center">
      <section className="h-12" aria-label="Wallet Address" hidden={!connected}>
        <AddrethConfig ens={true}>
          {client?.account?.address && (
            <Addreth
              ens={true}
              icon="ens"
              theme="dark"
              actions="none"
              address={client?.account?.address}
            />
          )}
        </AddrethConfig>
      </section>

      <button
        type="button"
        onClick={onConnectClick}
        className="rounded-sm hover:cursor-pointer bg-accent-800 text-white hover:bg-accent-700 font-bold uppercase text-sm px-2 py-1"
      >
        {connected ? "Disconnect" : "Connect"}
      </button>

<a target="_blank" rel="noreferrer noopener" href="https://app.union.build/explorer/transfers/0xb447bf681a50d9d6d64f442f7469a24abb7f8b2f2cd6d0a35700b43a92dad032">0xb447bf681a50d9d6d64f442f7469a24abb7f8b2f2cd6d0a35700b43a92dad032</a>
    </main>
  )
}
```
