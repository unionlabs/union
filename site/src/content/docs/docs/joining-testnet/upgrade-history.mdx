---
title: Upgrade History
sidebar:
  order: 10
---

import { dedent } from "ts-dedent"
import versions from "~root/versions/versions.json";
import Badge from "@astrojs/starlight/components/Badge.astro";
import { Steps, Card, Code } from "@astrojs/starlight/components";

Here you can find records of Union network binary upgrades.

{
  Object.entries(versions).map(([chainId, data]) => (
    <div>
      <h2 id={chainId}>
        <a href={`#${chainId}`}>{chainId}</a>
      </h2>
      {import.meta.env.CHAIN_ID_VERSION !== chainId && <Badge text="Deprecated" variant="caution" />}
      <p>
        The network {chainId} has had two binary upgrades since genesis.
      </p>
      <table>
        <thead><tr><th>Upgrade</th><th>Height</th></tr>
        </thead>
        <tbody>
          {data.versions.map((version, index) => (
          <tr>
            <td>{version}</td><td>{data.heights[index]}</td>
          </tr>
          ))}
        </tbody>
      </table>
    </div>
  ))
}
