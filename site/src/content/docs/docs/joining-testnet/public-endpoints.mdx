---
title: "Public Endpoints"
sidebar:
  order: 5
---

import endpoints from "#/content/openapi/endpoints.json";
import { Card, CardGrid } from "@astrojs/starlight/components";

These endpoints may be helpful while joining the network and ensuring the correctness of your node.

<Card title="gRPC">
  <ul>
    {endpoints.grpc.map(endpoint => (
      <li>
        <a href={endpoint} target="_blank" rel="noopener noreferrer">
          {endpoint}
        </a>
      </li>
    ))}
  </ul>
</Card>

<Card title="RPC">
  <ul>
    {endpoints.rpc.map(endpoint => (
      <li>
        <a href={endpoint} target="_blank" rel="noopener noreferrer">
          {endpoint}
        </a>
      </li>
    ))}
  </ul>
</Card>

<Card title="REST API">
  <ul>
    {endpoints.rest.map(endpoint => (
      <li>
        <a href={endpoint} target="_blank" rel="noopener noreferrer">
          {endpoint}
        </a>
      </li>
    ))}
  </ul>
</Card>

<Card title="State Sync">
  <ul>
    {endpoints["state-sync"].map(endpoint => (
      <li>
        <a href={endpoint} target="_blank" rel="noopener noreferrer">
          {endpoint}
        </a>
      </li>
    ))}
  </ul>
</Card>

<Card title="Explorer">
  <ul>
    {endpoints.explorers.map(endpoint => (
      <li>
        <a href={endpoint} target="_blank" rel="noopener noreferrer">
          {endpoint}
        </a>
      </li>
    ))}
  </ul>
</Card>
