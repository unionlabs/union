name: Build

on:
  push:
    branches:
    - main
  pull_request:
    branches:
    - main


concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.sha }}
  cancel-in-progress: true

jobs:
  spell-fmt-check:
    uses: unionlabs/workflows/.github/workflows/build.yml@3816360e7c993ef689e7c769f407ed481ea45f4a
    secrets:
      nixbuild_token: ${{ secrets.nixbuild_token }}
      access-tokens: github.com=${{ secrets.GITHUB_TOKEN }}
    with:
      filter_builds: |
        (
          (.top_attr == "checks") and
          (.system == "x86_64-linux") and 
          ( 
            (
              # Do not run on both systems
              [.attr] | inside(
                [
                  "spellcheck",
                  "treefmt-check"
                ]
              )
            )
          )
        )
  build:
    needs: [ spell-fmt-check ]
    if: |
      !cancelled()
      && (contains(needs.*.result, 'success') || github.event.pull_request.draft == true)
    uses: unionlabs/workflows/.github/workflows/build.yml@3816360e7c993ef689e7c769f407ed481ea45f4a
    secrets:
      nixbuild_token: ${{ secrets.nixbuild_token }}
      access-tokens: github.com=${{ secrets.GITHUB_TOKEN }}
    with:
      filter_builds: |
        (
          (.top_attr == "packages" or .top_attr == "devShells") and
          (.system == "x86_64-linux" or .system == "aarch64-linux") and 
          ( 
            (
              # Do not run on both systems
              [.attr] | inside(
                [
                  "uniond-release",
                  "devnet",
                  "devnet-cosmos",
                  "devnet-evm",
                  "docgen",
                  "download-circuit",
                  "evm",
                  "evm-contracts",
                  "fuzz",
                  "generate-evm-proto",
                  "generate-prover-proto",
                  "generate-rust-proto",
                  "gen-proto",
                  "githook",
                  "go-vendor",
                  "rust-proto",
                  "site-static",
                  "site-server",
                  "voyager-queue",
                  "sqlx-cli",
                  "fetchRustStdCargoLock",
                  "fetch-membership-data",
                  "gen-eth-lc-update-test-data",
                  "impure-site-deploy",
                  "rust-sol-bindings",
                  "vendor-tools",
                  "impure-site-deploy",
                  "generate-rust-sol-bindings"
                ]
              )
            ) or 
            (.attr | test("evm-.*-deploy")) | 
            not
          ) or 
          ( 
            # x86_64 only
            .system == "x86_64-linux" and 
            (
              [.attr] | inside(
                [
                  "devnet",
                  "devnet-cosmos",
                  "devnet-evm",
                  "evm",
                  "evm-contracts"
                ]
              )
            )
          )
        )
  test:
    needs: [ spell-fmt-check ]
    if: |
      !cancelled()
      && (contains(needs.*.result, 'success') || github.event.pull_request.draft == true)
    uses: unionlabs/workflows/.github/workflows/build.yml@3816360e7c993ef689e7c769f407ed481ea45f4a
    secrets:
      nixbuild_token: ${{ secrets.nixbuild_token }}
      access-tokens: github.com=${{ secrets.GITHUB_TOKEN }}
    with:
      filter_builds: |
        (
          (.top_attr == "checks") and
          (.system == "x86_64-linux") and 
          ( 
            (
              # Do not run
              [.attr] | inside(
                [
                  "ensure-blocks",
                  "epoch-completes",
                  "forced-set-rotation",
                  "pre-commit",
                  "pre-commit-check",
                  "sepolia-runs",
                  "spellcheck",
                  "treefmt-check",
                  "union-runs",
                  "upgrade-from-genesis",
                  "virtualisation-works",
                  "voyager-queue"
                ]
              )
            ) | not
          )
        )
