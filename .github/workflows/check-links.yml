name: Links Checker

on:
  workflow_dispatch:
  repository_dispatch:
  # daily check
  schedule:
    - cron: '00 18 * * *'

defaults:
  run:
    shell: bash

env:
  ACTIONS_RUNNER_DEBUG: true

jobs:
<<<<<<< HEAD
=======
  # runs on push and pull request
  link-checker:
    name: '🔗 Check Links'
    runs-on: ['ubuntu-latest']
    permissions:
      contents: read
      pull-requests: write
    if: github.event_name == 'push' || github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true

      - name: Link Check
        id: lychee
        uses: lycheeverse/lychee-action@v1.9.3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          args: |
            site/src app/src --config ${{ github.workspace }}/lychee.toml --github-token ${{ secrets.GITHUB_TOKEN }}

      - name: Comment On Pull Request
        uses: actions/github-script@v7
        # only comment if it's a pull request
        if: github.event_name == 'pull_request'
        env:
          DETECTED_BROKEN_LINKS: ${{ steps.lychee.outputs.exit_code != 0 }}
        # temporarily allow this to fail until we're able to handle these two links: https://github.com/unionlabs/union/actions/runs/8014925873/attempts/1#summary-21894317581
        continue-on-error: true
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('node:fs');
            const content = fs.readFileSync('./lychee/out.md', { encoding: 'utf8' });

            const prComments = await github.rest.issues.listComments({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo
            });
            const [existingComment] = prComments.data.filter(comment => comment.user.login === 'github-actions[bot]' && comment.body.includes('Summary'));
            
            if(existingComment) {
              await github.rest.issues.updateComment({
                comment_id: existingComment.id,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: content
              });
            } else {
              await github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: content
              });
            }

  # runs once daily
>>>>>>> 48400ae85 (fix: enable updating comment when no broken links)
  daily-link-checker:
    name: '🔗 Check Links'
    runs-on: ['ubuntu-latest']
    permissions:
      contents: read
      issues: write
    if: github.event_name == 'schedule'
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true

      - name: Install Nix
        uses: cachix/install-nix-action@v25
        with:
          nix_path: nixpkgs=channel:${{ env.NIXPKGS_CHANNEL }}
          github_access_token: ${{ github.token }}

      - name: Build Site
        run: nix build .#site

      - name: Link Check
        id: lychee
        uses: lycheeverse/lychee-action@v1.9.3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          args: |
            --no-progress
            'result/**/index.html'

      - name: Create Issue From File
        if: env.lychee_exit_code != 0
        uses: peter-evans/create-issue-from-file@v5
        with:
          title: Link Checker Report
          content-filepath: ./lychee/out.md
          labels: |
            documentation
            dead-links
